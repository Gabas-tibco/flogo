package cos

import (
	"testing"

	//"github.com/stretchr/testify/assert"
)

var ubp = &UniquizeByProperty{}

func TestUniquizerParser(t *testing.T) {
	

	/*result, _ := cj.Eval(csv, ";")

	structResult := result.(UniquizeByProperty)

	assert.Equal(t, len(structResult.Columns), 4)
	assert.Equal(t, len(structResult.Rows), 2)
	assert.Equal(t, len(structResult.Rows[0].Row), 4)*/
}
